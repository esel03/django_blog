meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: mysite/manage.py runserver 127.0.0.1:8000
  persistenceMount: /data
  containerPort: "80"
