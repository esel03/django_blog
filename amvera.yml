meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: mysite/manage.py runserver 0.0.0.0:80
  persistenceMount: /data
  containerPort: "80"
